/** @type {import('next').NextConfig} */
const nextConfig = {
    // images: {
    //     reactStrictMode: true,
    //     domains: ['res.cloudinary.com'],
    // },
    // target: 'serverless',
};

export default nextConfig;
